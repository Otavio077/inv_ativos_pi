Como iniciar o projeto:

- Instale as seguintes dependencias:
* pip install flask 
* pip install pymongo 
* pip install bson
* pip install requests

- Execute a API com "python api.py"
- Execute a interface "python interface.py"

Obs: o arquivo para criar o banco está incluso no projeto "criacao db.js"