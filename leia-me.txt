Como iniciar o projeto:

- Instale as seguintes dependencias:
* pip install flask 
* pip install pymongo 
* pip install bson
* pip install requests

- Execute o script "ciação db.js" no NoSQLBooster
- Execute a API com "python api.py"
- Execute a interface "python interface.py"